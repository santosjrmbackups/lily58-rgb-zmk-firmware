Flashable `.uf2` files are built by GitHub Actions (see the repo README).

Local copies (not committed):

- `oled-only-no-rgb/` — Typeractive stock Lily58 ZMK with OLED, **no RGB**. Only for bringing the screen up if you cannot wait for a build.
- `pandakb_lily58_RGB_vial-WIRED-RP2040-ONLY.uf2` — wired QMK/Vial. Use only if the bootloader drive is `RPI-RP2`.
